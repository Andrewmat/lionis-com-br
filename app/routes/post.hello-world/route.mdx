---
title: hello-world.mdx
description: Um post para falar sobre como fiz esse site
published: false
publishDate: 2024-10-07 18:51:00 -0300
tags: [tech]
summary: |
  This is a test
  of a multiline summary
---
import { getMeta, getLoader , PostHeader } from '~/lib/mdx-utils';
import { Button } from '~/components/ui/button'
import { Tabs, TabsList, TabsTrigger, TabsContent } from '~/components/ui/tabs';
import { TrashIcon } from 'lucide-react';

export const loader = getLoader(frontmatter);
export const meta = getMeta(frontmatter);

<PostHeader {...frontmatter} />

This is a test document. It has **bold** and _italic_ fonts, also `code` formatting\
and:

- lists
- with no
- order

## And headings too

1. also
2. ordered
3. lists

There are also [links](https://github.com) and [internal links](/).

<div className='flex gap-2'>
<Button type="button" className='mb-4' variant="default">Default buttons</Button>
<Button type="button" className='mb-4' variant="secondary">And also secondaries</Button>
<Button type="button" className='mb-4' variant="destructive">
  <TrashIcon />
</Button>
</div>

<div>
  <Tabs defaultValue='first'>
    <TabsList className='flex'>
      <TabsTrigger value="first" className='flex-1'>And also</TabsTrigger>
      <TabsTrigger value="second" className='flex-1'>tabs</TabsTrigger>
    </TabsList>
    <TabsContent value="first">
      Fully-functional...
    </TabsContent>
    <TabsContent value="second">
      ...tabs for you
    </TabsContent>
  </Tabs>
</div>

Temos textos <span className="inline-block bg-muted text-muted-foreground mb-4 rounded-lg p-2">mutado</span> também

```jsx
function itAlso(has) {
  return <Code support />;
}
```

```tsx
function itAlso(has: Props) {
  return <Code support />;
}
```

```ts
function itAlso(has: Props) {
  let value: number = 1;
}
```
